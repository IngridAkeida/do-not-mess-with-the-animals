/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['www.doesthedogdie.com', 'image.tmdb.org', 'media.guim.co.uk', 'firebasestorage.googleapis.com'],

  },
};

export default nextConfig;
